{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}Stock\ProductRawMaterial - {{ parent() }}{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            <a href="{{ path('stock_productrawmaterial') }}"> {{ 'Stock\ProductRawMaterial list'|trans({'Stock\ProductRawMaterial': 'Stock\ProductRawMaterial'}, 'Stock\ProductRawMaterial') }}</a>
            /
            {{ 'Stock\ProductRawMaterial' | trans({}, 'Stock\ProductRawMaterial')}}
        </h1>
    </section>
    <section class="content">    
        <div class="box"  id='showStock\ProductRawMaterial'>
            <div class="box-header">
                <div class="box-tools  pull-right">
                            {{ form_start(delete_form) }}
        {{ form_widget(delete_form) }}

        <div class="btn-group record_actions">            <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'Stock\ProductRawMaterial') }}</button>
            <input name="modal" type="hidden" value="0">
            
                <a class="btn btn-link  btn-sm" onclick="showEdit(true)">
                    {{ 'Edit'|trans({}, 'Stock\ProductRawMaterial') }}
                </a>
</div>
            <input name="modal" type="hidden" value="0">
        {{ form_end(delete_form) }}

</div>
            </div>
            <div class="box-body">
                <table class="table table-hover record_properties">
                    <tbody>                            
                        <tr>
                            <th scope="row">{{'Quantity'|trans({}, 'Stock\ProductRawMaterial')}}</th>
                            <td>{{ stock\productrawmaterial.quantity }}</td>
                        </tr>
                    </tbody>
                </table>
            </div>
            <div class="box-footer">
            
            </div>
        </div>
        <div class="box" id='editFormStock\ProductRawMaterial'  style="display: none;">
            <div class="box-header">
                <div class="box-tools">
                    {{ form_start(delete_form) }}
                    {{ form_widget(delete_form) }}
                    <div class="btn-group record_actions">
                        <button class="btn btn-link btn-sm" type="submit"> {{ 'Delete'|trans({}, 'Stock\ProductRawMaterial') }}</button>
                        <a class="btn btn-link  btn-sm" onclick="showEdit(false)">
                        {{ 'Cancel'|trans({}, 'Account') }}
                        </a>
                    </div>
                    <input name="modal" type="hidden" value="0">
                    {{ form_end(delete_form) }}
                </div>
            </div>
            <div class="box-body">

            {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn-default btn" type="submit">{{ 'save'|trans | capitalize}}</button>
                </p>
            {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
            </div>
        </div>
    </section>

        <div class="btn-group record_actions"></div>

        <div id="delete_confirm" class="modal fade">
            <div class="modal-dialog">
                <div class="modal-content">
                    <div class="modal-header">
                        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                        <h3>{{ 'Confirm delete'|trans({}, 'Stock\ProductRawMaterial') }}</h3>
                    </div>
                    <div class="modal-body">
                        <p>{{ 'You are about to delete an item'|trans({}, 'Stock\ProductRawMaterial') }}, {{ 'this procedure is irreversible'|trans({}, 'Stock\ProductRawMaterial') }}.</p>
                        <p>{{ 'Do you want to proceed?'|trans({}, 'Stock\ProductRawMaterial') }}</p>
                    </div>
                    <div class="modal-footer">
                        <button class="btn btn-danger">{{ 'Yes'|trans({}, 'Stock\ProductRawMaterial') }}</button>
                        <button class="btn btn-default" data-dismiss="modal">{{ 'No'|trans({}, 'Stock\ProductRawMaterial') }}</button>
                    </div>
                </div>
            </div>
        </div>
{% endblock %}
{% block javascripts %}

    {{ parent() }}
    <script type="text/javascript">
        function showEdit(show){
            if(show){
                $('#showStock\ProductRawMaterial').hide();
                $('#editFormStock\ProductRawMaterial').show();
            }else{
                $('#showStock\ProductRawMaterial').show();
                $('#editFormStock\ProductRawMaterial').hide();
            }
        }
        
    </script>
{% endblock %}