{% extends "FlowerCoreBundle::layout.html.twig" %}

{% block title %}Stock\RawMaterial - {{ parent() }}{% endblock %}


{% block app_menu %}
    {{ include("FlowerStockBundle::appnav.html.twig", { 'app_rawmaterial': true }) }}
{% endblock %}

{% block body %}
    <section class="content-header">
        <h1>
            <a href="{{ path('stock_rawmaterial') }}"> {{ 'Stock\RawMaterial list'|trans({'Stock\RawMaterial': 'Stock\RawMaterial'}, 'Stock\RawMaterial') }}</a>
            /
            {{ 'Stock\RawMaterial' | trans({}, 'Stock\RawMaterial') }}
        </h1>
    </section>
    <section class="content">
        <div class="box" id='showStock\RawMaterial'>
            <div class="box-header">
                <div class="box-tools  pull-right">
                    {{ form_start(delete_form) }}
                    {{ form_widget(delete_form) }}

                    <div class="btn-group record_actions">
                        <button class="btn btn-link btn-sm"
                                type="submit"> {{ 'Delete'|trans({}, 'Stock\RawMaterial') }}</button>
                        <input name="modal" type="hidden" value="0">

                        <a class="btn btn-link  btn-sm" onclick="showEdit(true)">
                            {{ 'Edit'|trans({}, 'Stock\RawMaterial') }}
                        </a>
                    </div>
                    <input name="modal" type="hidden" value="0">
                    {{ form_end(delete_form) }}

                </div>
            </div>
            <div class="box-body">
                <table class="table table-hover record_properties">
                    <tbody>
                    <tr>
                        <th scope="row">{{ 'Name'|trans({}, 'Stock\RawMaterial') }}</th>
                        <td>{{ rawmaterial.name }}</td>
                    </tr>
                    <tr>
                        <th scope="row">{{ 'Stock'|trans({}, 'Stock\RawMaterial') }}</th>
                        <td>{{ rawmaterial.stock }}</td>
                    </tr>
                    <tr>
                        <th scope="row">{{ 'Enabled'|trans({}, 'Stock\RawMaterial') }}</th>
                        <td>{% if rawmaterial.enabled %}<i class="fa fa-check-square-o"></i>{% else %}<i
                                    class="fa fa-square-o"></i>{% endif %}</td>
                    </tr>
                    </tbody>
                </table>
            </div>
            <div class="box-footer">

            </div>
        </div>
        <div class="box" id='editFormStock\RawMaterial' style="display: none;">
            <div class="box-header">
                <div class="box-tools">
                    {{ form_start(delete_form) }}
                    {{ form_widget(delete_form) }}
                    <div class="btn-group record_actions">
                        <button class="btn btn-link btn-sm"
                                type="submit"> {{ 'Delete'|trans({}, 'Stock\RawMaterial') }}</button>
                        <a class="btn btn-link  btn-sm" onclick="showEdit(false)">
                            {{ 'Cancel'|trans({}, 'Account') }}
                        </a>
                    </div>
                    <input name="modal" type="hidden" value="0">
                    {{ form_end(delete_form) }}
                </div>
            </div>
            <div class="box-body">

                {{ form_start(edit_form) }}
                {{ form_widget(edit_form) }}
                <p>
                    <button class="btn-default btn" type="submit">{{ 'save'|trans | capitalize }}</button>
                </p>
                {{ form_end(edit_form) }}
            </div>
            <div class="box-footer">
            </div>
        </div>
    </section>

    <div class="btn-group record_actions"></div>

    <div id="delete_confirm" class="modal fade">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal" aria-hidden="true">&times;</button>
                    <h3>{{ 'Confirm delete'|trans({}, 'Stock\RawMaterial') }}</h3>
                </div>
                <div class="modal-body">
                    <p>{{ 'You are about to delete an item'|trans({}, 'Stock\RawMaterial') }}
                        , {{ 'this procedure is irreversible'|trans({}, 'Stock\RawMaterial') }}.</p>
                    <p>{{ 'Do you want to proceed?'|trans({}, 'Stock\RawMaterial') }}</p>
                </div>
                <div class="modal-footer">
                    <button class="btn btn-danger">{{ 'Yes'|trans({}, 'Stock\RawMaterial') }}</button>
                    <button class="btn btn-default"
                            data-dismiss="modal">{{ 'No'|trans({}, 'Stock\RawMaterial') }}</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
{% block javascripts %}

    {{ parent() }}
    <script type="text/javascript">
        function showEdit(show) {
            if (show) {
                $('#showStock\RawMaterial').hide();
                $('#editFormStock\RawMaterial').show();
            } else {
                $('#showStock\RawMaterial').show();
                $('#editFormStock\RawMaterial').hide();
            }
        }

    </script>
{% endblock %}